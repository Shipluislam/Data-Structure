/// Binary search.
#include<iostream>
using namespace std;
int main()
{
    int i,a[]={2,3,4,5,6,7,8,9};
    cin>>i;
    int b=0;
    int e=7;
    int m;
    while(b<=e && a[m]!=i)
    {
        m=int(b+e)/2;
        if(i<a[m])
        {
            e=m-1;
        }
        else
        {
            b=m+1;
        }
    }
    if(i==a[m])
    {
        cout<<"Date is found at mid";
    }
    else
    {
        cout<<"Data is not found at mid";
    }
    return 0;
}
